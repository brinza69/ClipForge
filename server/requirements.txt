# ClipForge Worker - Phase 1 (metadata preview foundation)
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.34.0,<1.0
pydantic>=2.10.0,<3.0
pydantic-settings>=2.7.0,<3.0
sqlalchemy[asyncio]>=2.0.36,<3.0
aiosqlite>=0.21.0,<1.0
yt-dlp>=2025.3.0
httpx>=0.28.0,<1.0
faster-whisper>=1.0.0
pysubs2>=1.6.0
opencv-python>=4.9.0
# OCR for auto-detecting on-screen captions (downloads ~200MB of model weights on first use)
easyocr>=1.7.1
# Google Drive upload (optional — backend tolerates missing imports)
google-api-python-client>=2.100.0
google-auth>=2.25.0
# OAuth user-account flow for Drive (uploads as the user → uses their 15GB quota)
google-auth-oauthlib>=1.2.0
# Form-data parsing for FastAPI file uploads (commentator / caption-eraser endpoints)
python-multipart>=0.0.9
# Audio silence trimming for the TTS post-processing chain
pydub>=0.25.1
# pydub imports the stdlib `audioop` module, which was REMOVED in Python 3.13 —
# this backport restores it. No-op / not needed on Python <= 3.12.
audioop-lts>=0.2; python_version >= "3.13"
# NOTE: GPU neural inpainting also needs torch + torchvision (CUDA build) and
# simple-lama-inpainting. Those are NOT here because the CUDA wheels come from
# the PyTorch index (not PyPI). Install them with scripts/setup-gpu.(ps1|sh).
# Without them the eraser still works on CPU via OpenCV (telea), just slower.
#
# Auto Story Doodle Video — local Kokoro TTS voiceover (server/services/doodle/kokoro_service.py).
# Uses the torch already installed above (CUDA build) — do NOT let this pull a
# different torch wheel. kokoro pulls transformers/tokenizers/huggingface-hub;
# on this venv we pinned transformers==4.49.0 + tokenizers>=0.21,<0.22 +
# huggingface-hub>=0.26,<1.0 for compatibility (faster-whisper tolerates both).
# misaki[en] (kokoro's English G2P) additionally needs spacy + phonemizer +
# their transitive deps (segments, csvw, ...) — installed alongside.
kokoro>=0.7.16
soundfile>=0.14.0
# Bundles a portable espeak-ng library/data so misaki's espeak fallback works
# without a system-wide espeak-ng MSI install. If phonemizer still fails to
# init at runtime, install the espeak-ng MSI from
# https://github.com/espeak-ng/espeak-ng/releases as a fallback.
espeakng-loader>=0.2.4
