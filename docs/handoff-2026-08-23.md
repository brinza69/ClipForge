# Handoff — seara de 23 august 2026

Calculatorul a fost oprit la cerere. Ce urmeaza mai jos e ce trebuie reluat maine.

## Reia de aici

1. Porneste rigul (`scripts/rig_boot.ps1` ruleaza singur la logon).
2. Reia **faza 3** — povestitor ENGLEZ, 20 de randuri ramase (vezi mai jos).
3. Ruleaza **refacerea sunetului** pe cele 89 de fisiere identificate.
4. Re-randeaza **NR 79** (francez) — era in coada cand s-a oprit.

## Ce s-a schimbat in cod in seara asta

| ce | unde | de ce |
|---|---|---|
| profil de encodare "ca la CapCut" | `services/encode_profile.py` (nou), legat in `remix_pipeline`, `parallel_pipeline._split_video`, `commentator_overlay` | H.264 **Main**, **14,1 Mbps**, GOP **fix** la 2,5 s, 60 fps. Fisierul trece prin TREI encodari si ultima le rescrie pe primele — de aia toate trei folosesc acelasi profil. Verificat pe fisier: profil Main, 14,06 Mbps, cadre cheie la 2,50 s constant. |
| sunetul sursei ANULAT peste tot | `bg_music_db: ""` pe **toate** presetele, in `data/` si `data_b/`; `CLIPFORGE_BG_MUSIC_DB` golit in `rig_state.json`; `_bg_audio_filter()` accepta override per preset | Ramane **doar vocea personajului**. Presetele se citesc de pe disc la fiecare job, deci schimbarea nu cere repornire — variabila de mediu, da. |
| benzi MULTIPLE de subtitrare | `caption_detector.auto_locate_caption_bands()` (nou, plural) + bucla in `remix_pipeline._stage_erase` | Detectorul grupa deja textul in benzi si le dadea scor, dar returna **doar pe cea mai buna**. Un clip cu disclaimer sus + subtitrare mai jos ramanea cu a doua nesearsa. Pragul: 20% din scorul maxim. |
| espeak pentru Kokoro | `kokoro_tts._leaga_espeak()` | Fara rezerva, orice nume propriu necunoscut picca randarea la 48%, dupa ce se platise transcrierea. Binarul venea deja cu `espeakng_loader`; misaki il cauta doar in `C:\Program Files`. |
| outro englezesc | `CLIPFORGE_TTS_OUTRO_EN` in `rig_state.py` (cheie noua + `--outro-en`) | Engleza cadea pe outro-ul romanesc si Kokoro il citea fonetic: *"The likesy-follow doc of Ray Savezi"*. |
| motor F5-TTS romana | `services/f5_ro_tts.py` (nou), engine `f5_ro` | Clonare zero-shot in romana, gratuit. Instalat in `.venv_f5`, model in `data/models/f5_ro/`. Masurat WER 2%. **Nelegat inca in frontend.** |

**Nimic nu e comis.** Toate schimbarile sunt in working tree.

## Ce rula cand s-a oprit (se pierde, trebuie reluat)

- `e5af84517ff5` — NR 194, povestitor englez, era la 23%
- `836247d477d5` — NR 199, povestitor englez, era la 72%
- `ea8eb6d6810a` — **NR 79 francez**, in coada, 0%

Backend-urile au recuperare automata la pornire ("Stuck-job recovery"), deci s-ar putea sa reia singure. Verifica intai.

## Faza 3 — povestitor englez, 20 de randuri ramase

Preset `povestitor_en` (Kokoro `am_adam`, engleza, **zero credite**), folder Drive
separat **Povestitor EN** `targets.json` -> `povestitor_en_drive_folder`.

Folderul separat e intentionat: filtrul de limba al lui `build_pov_post_list.py`
se bazeaza pe `pov_inventory.json`, un inventar manual. Randarile engleze n-ar fi
in el si ar intra in planul ROMANESC, ajungand pe Povestitorul (Facebook) cu
descriere romaneasca peste audio englezesc.

Randuri: NR 200-206, 252-260 (din HisYTStory) + NR 270-274.
Gata deja: 191, 192, 193. In curs cand s-a oprit: 194, 199.

Runner: `scratchpad/faza3.py` (nu supravietuieste repornirii — de rescris sau
de rulat manual).

## Refacerea sunetului — 89 de fisiere, ZERO credite

**Cum merge fara re-randare:** pipeline-ul pastreaza `voice.wav` in fiecare
director de varianta — exact pista care a intrat in encodare, curata. Se ia
fluxul VIDEO neatins (`-c:v copy`, deci profilul CapCut ramane intact) si i se
pune vocea curata in loc de mixul cu sunetul sursei.

Fisierul de pe Drive se actualizeaza **pe loc, cu acelasi ID** — postarile deja
programate in Buffer trimit la ID, deci preiau noul sunet **fara reprogramare**.

- **49** identificate direct (Victoria FR + povestitor 265, 266) — `scratchpad/plan_remux.json`
- **+40** din folderul `posted/`, care e subfolder al **povestitorului**, deci toate
  sunt variante povestitor si se pot potrivi fara ambiguitate
- **11** (Victoria `1.mp4`-`12.mp4`) nu se pot: proiectele au fost curatate, vocea nu mai exista
- **270** e in lista de facut — utilizatorul a confirmat ca se aude sunetul original

### CAPCANA care a produs o greseala in seara asta

Potrivirea fisierului de pe Drive cu sursa locala **NU se face pe nume**.
`262.mp4` exista in patru foldere (narator, comentator, povestitor, posted/) cu
**continut diferit**. Am cautat pe nume global si am urcat versiunea povestitor
peste narator si comentator. Reparat, dar de evitat.

Potrivirea corecta: **(folder parinte pe Drive + nume fisier)**, folosind
`metadata_json` din tabela `jobs` — fiecare varianta isi declara `drive_folder`.
Script: `scratchpad/plan_remux.py`.

| varianta locala | personaj | folder Drive |
|---|---|---|
| `<proiect>/v0` al jobului cu 2 variante | Narator | `targets.json` -> `narator_drive_folder` |
| `<proiect>/v1` al aceluiasi job | Comentator | `targets.json` -> `comentator_drive_folder` |
| proiect separat, `v0` | Povestitor | `targets.json` -> `povestitor_drive_folder` |
| — | Victoria (FR) | `targets.json` -> `fr_drive_folder` |
| — | Povestitor EN | `targets.json` -> `povestitor_en_drive_folder` |

`266` (narator) e bun — model de referinta. `270` NU e bun, se aude sursa.

## Credite ElevenLabs

**1.833 ramase**, resetare **8 septembrie 2026, 20:50**.

Consum masurat: francez ~350/randare, roman narator/comentator ~450/rol.
Deci mai incap ~4 randari. Refacerea sunetului **nu consuma nimic**.

Utilizatorul a dat o **cheie noua** de aplicat cand se epuizeaza cea curenta.
Nu e salvata nicaieri — o va da din nou. (A fost lipita in chat, deci merita
rotita din ElevenLabs.)

## Buffer

**105 postari programate.** Nimic nu cade in urmatoarele ~2 zile.
Cele 12 esuate au fost reprogramate (11 reusit, 1 pusa ca postare in feed
fiindca raportul nu trecea la Facebook Reels).

Rata de esec ~9%, si **nu e legata de marime**: esuate 24-80 MB, reusite 9-121 MB,
aceeasi medie de 54 MB.

De stiut: `deletePost` foloseste uniunea `DeletePostSuccess | VoidMutationError`,
nu `PostActionSuccess`. Iar reprogramarea cere metadata per platforma — YouTube
vrea titlu + `categoryId`, Facebook vrea `type`.

## Randuri adaugate in seara asta

- Sheet romanesc: **271-273** (NR 267-269) si **274-278** (NR 270-274)
- Victoria: **70-77** (NR 67-74, gata) si **78-82** (NR 75-79; 79 de refacut)
- `69tlbu-ppUI` e **mort** — sters de pe YouTube, rand 59 in Victoria

## Instabilitate de retea

Toata ziua au aparut timeout-uri la Google (Sheets si Drive). Una a produs un
**fals negativ**: pipeline-ul a raportat `upload failed` pentru NR 77, dar
fisierul era pe Drive, complet. Daca vezi asta, **verifica Drive-ul inainte sa
re-randezi**.
